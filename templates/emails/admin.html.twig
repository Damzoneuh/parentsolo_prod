<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        {{ encore_entry_link_tags('global') }}
    </head>
    <body>
    <div class="w-50">
        <div style="text-align: center; padding: 10px" >
            <div style="border: 1px solid red; border-radius: 1em;">
                <div style="text-align: center; margin: 20px"><img src="{{ absolute_url(asset('/img/FavIcon.png')) }}" alt="logo" class="w-50"></div>
                <div style="text-align: center; margin: 20px"><h1 style="color: grey">{{ title }}</h1></div>
                <div style="display: flex; align-items: center; justify-content: space-around; margin-bottom: 10px">
                    <div >Email du demandeur : {{ email }}</div>
                </div>
                <div style="display: flex; align-items: center; justify-content: space-around; margin-bottom: 10px">
                    <div>Message : {{ message }}</div>
                </div>
            </div>
        </div>
    </div>
    </body>
</html>